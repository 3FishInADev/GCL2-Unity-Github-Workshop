using UnityEngine;

public class FernControl : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hi peeps");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

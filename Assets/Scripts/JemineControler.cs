using UnityEngine;

public class JemineControler : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("HI I want to sleep and I am the Floor, Yahoo!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
